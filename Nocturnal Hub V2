local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local gui = Instance.new("ScreenGui")
gui.Name = "NocturnalUI"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = playerGui

local function tween(obj, time, props, style, direction)
	return TweenService:Create(
		obj,
		TweenInfo.new(
			time,
			style or Enum.EasingStyle.Quart,
			direction or Enum.EasingDirection.Out
		),
		props
	)
end

local BG = Color3.fromRGB(12, 13, 16)
local PANEL = Color3.fromRGB(17, 19, 23)
local CARD = Color3.fromRGB(23, 25, 30)
local WHITE = Color3.fromRGB(242, 243, 246)
local MUTED = Color3.fromRGB(120, 124, 133)
local DIM = Color3.fromRGB(76, 80, 89)
local GREEN = Color3.fromRGB(75, 210, 115)
local RED = Color3.fromRGB(235, 75, 75)

local function round(obj, radius)
	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0, radius)
	c.Parent = obj
	return c
end

local function stroke(obj, color, transparency)
	local s = Instance.new("UIStroke")
	s.Color = color
	s.Transparency = transparency or 0
	s.Parent = obj
	return s
end

-- Loading

local loading = Instance.new("Frame")
loading.Size = UDim2.fromScale(1, 1)
loading.BackgroundTransparency = 1
loading.BorderSizePixel = 0
loading.Parent = gui

local loadingTitle = Instance.new("TextLabel")
loadingTitle.AnchorPoint = Vector2.new(.5, .5)
loadingTitle.Position = UDim2.fromScale(.5, .47)
loadingTitle.Size = UDim2.fromOffset(500, 55)
loadingTitle.BackgroundTransparency = 1
loadingTitle.Text = "nocturnal hub :3"
loadingTitle.TextColor3 = WHITE
loadingTitle.TextSize = 38
loadingTitle.Font = Enum.Font.GothamBold
loadingTitle.TextTransparency = 1
loadingTitle.Parent = loading

local loadingSub = Instance.new("TextLabel")
loadingSub.AnchorPoint = Vector2.new(.5, .5)
loadingSub.Position = UDim2.fromScale(.5, .515)
loadingSub.Size = UDim2.fromOffset(400, 24)
loadingSub.BackgroundTransparency = 1
loadingSub.Text = "initializing"
loadingSub.TextColor3 = MUTED
loadingSub.TextSize = 11
loadingSub.Font = Enum.Font.GothamMedium
loadingSub.TextTransparency = 1
loadingSub.Parent = loading

local loadingBar = Instance.new("Frame")
loadingBar.AnchorPoint = Vector2.new(.5, .5)
loadingBar.Position = UDim2.fromScale(.5, .56)
loadingBar.Size = UDim2.fromOffset(280, 5)
loadingBar.BackgroundColor3 = Color3.fromRGB(35, 37, 42)
loadingBar.BackgroundTransparency = 1
loadingBar.BorderSizePixel = 0
loadingBar.ClipsDescendants = true
loadingBar.Parent = loading

round(loadingBar, 5)

local loadingFill = Instance.new("Frame")
loadingFill.Size = UDim2.new(0, 0, 1, 0)
loadingFill.BackgroundColor3 = WHITE
loadingFill.BorderSizePixel = 0
loadingFill.Parent = loadingBar

round(loadingFill, 5)

tween(loadingTitle, .6, {TextTransparency = 0}):Play()
tween(loadingSub, .6, {TextTransparency = 0}):Play()
tween(loadingBar, .6, {BackgroundTransparency = 0}):Play()

local loadTween = tween(
	loadingFill,
	1.8,
	{Size = UDim2.fromScale(1, 1)},
	Enum.EasingStyle.Quart
)

loadTween:Play()
loadTween.Completed:Wait()

task.wait(.15)

tween(loadingTitle, .35, {TextTransparency = 1}):Play()
tween(loadingSub, .35, {TextTransparency = 1}):Play()
tween(loadingBar, .35, {BackgroundTransparency = 1}):Play()
tween(loadingFill, .35, {BackgroundTransparency = 1}):Play()

task.wait(.4)
loading:Destroy()

-- Main

local MAIN_SIZE = UDim2.fromOffset(560, 390)

local main = Instance.new("Frame")
main.Name = "Main"
main.Size = MAIN_SIZE
main.Position = UDim2.fromScale(.5, .5)
main.AnchorPoint = Vector2.new(.5, .5)
main.BackgroundColor3 = BG
main.BackgroundTransparency = .02
main.BorderSizePixel = 0
main.ClipsDescendants = true
main.Parent = gui

round(main, 18)
local mainStroke = stroke(main, Color3.fromRGB(65, 68, 76), .35)

-- Header

local brand = Instance.new("TextLabel")
brand.Position = UDim2.fromOffset(22, 16)
brand.Size = UDim2.fromOffset(160, 18)
brand.BackgroundTransparency = 1
brand.Text = "NOCTURNAL"
brand.TextColor3 = MUTED
brand.TextSize = 10
brand.Font = Enum.Font.GothamBold
brand.TextXAlignment = Enum.TextXAlignment.Left
brand.Parent = main

local title = Instance.new("TextLabel")
title.Position = UDim2.fromOffset(22, 35)
title.Size = UDim2.fromOffset(280, 30)
title.BackgroundTransparency = 1
title.Text = "Script Hub"
title.TextColor3 = WHITE
title.TextSize = 22
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = main

local subtitle = Instance.new("TextLabel")
subtitle.Position = UDim2.fromOffset(23, 62)
subtitle.Size = UDim2.fromOffset(300, 18)
subtitle.BackgroundTransparency = 1
subtitle.Text = "welcome back"
subtitle.TextColor3 = MUTED
subtitle.TextSize = 10
subtitle.Font = Enum.Font.GothamMedium
subtitle.TextXAlignment = Enum.TextXAlignment.Left
subtitle.Parent = main

local minimize = Instance.new("TextButton")
minimize.AnchorPoint = Vector2.new(1, 0)
minimize.Position = UDim2.new(1, -48, 0, 15)
minimize.Size = UDim2.fromOffset(28, 28)
minimize.BackgroundTransparency = 1
minimize.Text = "−"
minimize.TextColor3 = WHITE
minimize.TextSize = 20
minimize.Font = Enum.Font.GothamMedium
minimize.AutoButtonColor = false
minimize.Parent = main

local close = Instance.new("TextButton")
close.AnchorPoint = Vector2.new(1, 0)
close.Position = UDim2.new(1, -14, 0, 15)
close.Size = UDim2.fromOffset(28, 28)
close.BackgroundTransparency = 1
close.Text = "×"
close.TextColor3 = WHITE
close.TextSize = 21
close.Font = Enum.Font.GothamMedium
close.AutoButtonColor = false
close.Parent = main

minimize.MouseEnter:Connect(function()
	tween(minimize, .15, {TextColor3 = GREEN}):Play()
end)

minimize.MouseLeave:Connect(function()
	tween(minimize, .15, {TextColor3 = WHITE}):Play()
end)

close.MouseEnter:Connect(function()
	tween(close, .15, {TextColor3 = RED}):Play()
end)

close.MouseLeave:Connect(function()
	tween(close, .15, {TextColor3 = WHITE}):Play()
end)

-- Sidebar

local sidebar = Instance.new("Frame")
sidebar.Position = UDim2.fromOffset(15, 92)
sidebar.Size = UDim2.fromOffset(125, 278)
sidebar.BackgroundColor3 = Color3.fromRGB(15, 17, 20)
sidebar.BackgroundTransparency = .25
sidebar.BorderSizePixel = 0
sidebar.Parent = main

round(sidebar, 13)
stroke(sidebar, Color3.fromRGB(52, 55, 62), .55)

-- Content

local content = Instance.new("Frame")
content.Position = UDim2.fromOffset(152, 92)
content.Size = UDim2.new(1, -167, 1, -107)
content.BackgroundTransparency = 1
content.ClipsDescendants = true
content.Parent = main

local ICONS = {
	Home = "🏠",
	TSB = "⚔️",
	VBL = "🏐",
	General = "🌐",
	JJS = "🎮",
	Settings = "⚙️"
}

local tabs = {}
local pages = {}

local activePill = Instance.new("Frame")
activePill.BackgroundColor3 = WHITE
activePill.BorderSizePixel = 0
activePill.Size = UDim2.fromOffset(108, 40)
activePill.Position = UDim2.fromOffset(8, 8)
activePill.ZIndex = 1
activePill.Parent = sidebar

round(activePill, 11)

local function createPage(name)
	local page = Instance.new("ScrollingFrame")
	page.Name = name
	page.Size = UDim2.fromScale(1, 1)
	page.BackgroundTransparency = 1
	page.BorderSizePixel = 0
	page.ScrollBarThickness = 3
	page.ScrollBarImageColor3 = DIM
	page.CanvasSize = UDim2.new(0, 0, 0, 0)
	page.AutomaticCanvasSize = Enum.AutomaticSize.Y
	page.ScrollingDirection = Enum.ScrollingDirection.Y
	page.Visible = false
	page.Parent = content

	local padding = Instance.new("UIPadding")
	padding.PaddingLeft = UDim.new(0, 5)
	padding.PaddingRight = UDim.new(0, 5)
	padding.PaddingTop = UDim.new(0, 5)
	padding.PaddingBottom = UDim.new(0, 10)
	padding.Parent = page

	pages[name] = page
	return page
end

local function createTab(name, order)
	local button = Instance.new("TextButton")
	button.Name = name
	button.Size = UDim2.fromOffset(108, 40)
	button.Position = UDim2.fromOffset(8, 8 + ((order - 1) * 48))
	button.BackgroundTransparency = 1
	button.BorderSizePixel = 0
	button.Text = ""
	button.AutoButtonColor = false
	button.ZIndex = 2
	button.Parent = sidebar

	local icon = Instance.new("TextLabel")
	icon.Position = UDim2.fromOffset(11, 8)
	icon.Size = UDim2.fromOffset(23, 23)
	icon.BackgroundTransparency = 1
	icon.Text = ICONS[name]
	icon.TextColor3 = WHITE
	icon.TextSize = 17
	icon.Font = Enum.Font.Gotham
	icon.ZIndex = 3
	icon.Parent = button

	local label = Instance.new("TextLabel")
	label.Position = UDim2.fromOffset(38, 0)
	label.Size = UDim2.new(1, -43, 1, 0)
	label.BackgroundTransparency = 1
	label.Text = name
	label.TextColor3 = MUTED
	label.TextSize = 11
	label.Font = Enum.Font.GothamMedium
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.ZIndex = 3
	label.Parent = button

	tabs[name] = {
		Button = button,
		Label = label
	}
end

local homePage = createPage("Home")
local tsbPage = createPage("TSB")
local vblPage = createPage("VBL")
local generalPage = createPage("General")
local jjsPage = createPage("JJS")
local settingsPage = createPage("Settings")

createTab("Home", 1)
createTab("TSB", 2)
createTab("VBL", 3)
createTab("General", 4)
createTab("JJS", 5)
createTab("Settings", 6)

-- Home

local welcome = Instance.new("TextLabel")
welcome.Position = UDim2.fromOffset(5, 5)
welcome.Size = UDim2.new(1, -10, 0, 30)
welcome.BackgroundTransparency = 1
welcome.Text = "Welcome back, " .. player.DisplayName .. " :)"
welcome.TextColor3 = WHITE
welcome.TextSize = 20
welcome.Font = Enum.Font.GothamBold
welcome.TextXAlignment = Enum.TextXAlignment.Left
welcome.Parent = homePage

local welcomeSub = Instance.new("TextLabel")
welcomeSub.Position = UDim2.fromOffset(6, 35)
welcomeSub.Size = UDim2.new(1, -10, 0, 20)
welcomeSub.BackgroundTransparency = 1
welcomeSub.Text = "your player overview"
welcomeSub.TextColor3 = MUTED
welcomeSub.TextSize = 10
welcomeSub.Font = Enum.Font.GothamMedium
welcomeSub.TextXAlignment = Enum.TextXAlignment.Left
welcomeSub.Parent = homePage

local profile = Instance.new("Frame")
profile.Position = UDim2.fromOffset(5, 70)
profile.Size = UDim2.new(1, -10, 0, 135)
profile.BackgroundColor3 = CARD
profile.BorderSizePixel = 0
profile.Parent = homePage

round(profile, 14)
stroke(profile, Color3.fromRGB(59, 62, 70), .5)

local avatar = Instance.new("ImageLabel")
avatar.Position = UDim2.fromOffset(15, 15)
avatar.Size = UDim2.fromOffset(95, 95)
avatar.BackgroundColor3 = Color3.fromRGB(29, 31, 36)
avatar.BorderSizePixel = 0
avatar.Parent = profile

round(avatar, 12)

local thumb = Players:GetUserThumbnailAsync(
	player.UserId,
	Enum.ThumbnailType.AvatarBust,
	Enum.ThumbnailSize.Size150x150
)

avatar.Image = thumb

local username = Instance.new("TextLabel")
username.Position = UDim2.fromOffset(130, 18)
username.Size = UDim2.new(1, -145, 0, 24)
username.BackgroundTransparency = 1
username.Text = "@" .. player.Name
username.TextColor3 = WHITE
username.TextSize = 16
username.Font = Enum.Font.GothamBold
username.TextXAlignment = Enum.TextXAlignment.Left
username.Parent = profile

local displayName = Instance.new("TextLabel")
displayName.Position = UDim2.fromOffset(130, 43)
displayName.Size = UDim2.new(1, -145, 0, 20)
displayName.BackgroundTransparency = 1
displayName.Text = player.DisplayName
displayName.TextColor3 = MUTED
displayName.TextSize = 11
displayName.Font = Enum.Font.GothamMedium
displayName.TextXAlignment = Enum.TextXAlignment.Left
displayName.Parent = profile

local pingLabel = Instance.new("TextLabel")
pingLabel.Position = UDim2.fromOffset(130, 75)
pingLabel.Size = UDim2.new(1, -145, 0, 22)
pingLabel.BackgroundTransparency = 1
pingLabel.Text = "Ping: -- ms"
pingLabel.TextColor3 = GREEN
pingLabel.TextSize = 11
pingLabel.Font = Enum.Font.GothamMedium
pingLabel.TextXAlignment = Enum.TextXAlignment.Left
pingLabel.Parent = profile

local fpsLabel = Instance.new("TextLabel")
fpsLabel.Position = UDim2.fromOffset(130, 99)
fpsLabel.Size = UDim2.new(1, -145, 0, 20)
fpsLabel.BackgroundTransparency = 1
fpsLabel.Text = "FPS: --"
fpsLabel.TextColor3 = Color3.fromRGB(105, 108, 115)
fpsLabel.TextSize = 11
fpsLabel.Font = Enum.Font.GothamMedium
fpsLabel.TextXAlignment = Enum.TextXAlignment.Left
fpsLabel.Parent = profile

local status = Instance.new("TextLabel")
status.Position = UDim2.fromOffset(5, 220)
status.Size = UDim2.new(1, -10, 0, 45)
status.BackgroundColor3 = CARD
status.BorderSizePixel = 0
status.Text = "●  Connected"
status.TextColor3 = GREEN
status.TextSize = 11
status.Font = Enum.Font.GothamMedium
status.TextXAlignment = Enum.TextXAlignment.Left
status.Parent = homePage

local statusPadding = Instance.new("UIPadding")
statusPadding.PaddingLeft = UDim.new(0, 16)
statusPadding.Parent = status

round(status, 11)

--ping and fps updater

task.spawn(function()
	local frameCount = 0
	local lastUpdate = os.clock()

	RunService.RenderStepped:Connect(function()
		frameCount += 1
	end)

	while gui.Parent do
		task.wait(1)

		local now = os.clock()
		local elapsed = now - lastUpdate

		local fps = math.round(frameCount / math.max(elapsed, 0.001))

		frameCount = 0
		lastUpdate = now

		local ping = 0

		pcall(function()
			ping = math.round(player:GetNetworkPing() * 1000)
		end)

		fpsLabel.Text = "FPS: " .. fps
		pingLabel.Text = "Ping: " .. ping .. " ms"

		-- FPS color
		if fps >= 60 then
			fpsLabel.TextColor3 = Color3.fromRGB(105, 108, 115)
		elseif fps >= 30 then
			fpsLabel.TextColor3 = Color3.fromRGB(180, 180, 135)
		else
			fpsLabel.TextColor3 = Color3.fromRGB(205, 105, 105)
		end

		-- Ping color
		if ping <= 80 then
			pingLabel.TextColor3 = Color3.fromRGB(105, 108, 115)
		elseif ping <= 150 then
			pingLabel.TextColor3 = Color3.fromRGB(180, 180, 135)
		else
			pingLabel.TextColor3 = Color3.fromRGB(205, 105, 105)
		end
	end
end)



-- Helpers

local function createHubButton(parent, text, callback)
	local button = Instance.new("TextButton")
	button.Size = UDim2.new(1, -10, 0, 45)
	button.BackgroundColor3 = Color3.fromRGB(25, 27, 30)
	button.BackgroundTransparency = 0.12
	button.BorderSizePixel = 0
	button.Text = ""
	button.AutoButtonColor = false
	button.Parent = parent

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 10)
	corner.Parent = button

	local buttonStroke = Instance.new("UIStroke")
	buttonStroke.Color = Color3.fromRGB(66, 69, 76)
	buttonStroke.Thickness = 1
	buttonStroke.Transparency = 0.4
	buttonStroke.Parent = button

	-- Subtle vertical glass gradient
	local gradient = Instance.new("UIGradient")
	gradient.Rotation = 90
	gradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(46, 48, 53)),
		ColorSequenceKeypoint.new(0.5, Color3.fromRGB(27, 29, 32)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 22, 24))
	})

	gradient.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.4),
		NumberSequenceKeypoint.new(0.5, 0.72),
		NumberSequenceKeypoint.new(1, 0.25)
	})

	gradient.Parent = button

	-- Button text
	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, -65, 1, 0)
	label.Position = UDim2.fromOffset(17, 0)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = Color3.fromRGB(215, 217, 222)
	label.TextSize = 12
	label.Font = Enum.Font.GothamMedium
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = button

	-- Nocturnal N
	local n = Instance.new("TextLabel")
	n.AnchorPoint = Vector2.new(1, 0.5)
	n.Position = UDim2.new(1, -16, 0.5, 0)
	n.Size = UDim2.fromOffset(24, 24)
	n.BackgroundTransparency = 1
	n.Text = "N"
	n.TextColor3 = Color3.fromRGB(105, 108, 115)
	n.TextSize = 13
	n.FontFace = Font.new(
		"rbxasset://fonts/families/GothamSSm.json",
		Enum.FontWeight.Bold,
		Enum.FontStyle.Normal
	)
	n.Parent = button

	-- Slow N rotation
	task.spawn(function()
		while button.Parent do
			local rotationTween = tween(
				n,
				5,
				{
					Rotation = n.Rotation + 360
				},
				Enum.EasingStyle.Linear
			)

			rotationTween:Play()
			rotationTween.Completed:Wait()
		end
	end)

	-- Hover
	button.MouseEnter:Connect(function()
		tween(button, 0.18, {
			BackgroundTransparency = 0.02
		}):Play()

		tween(buttonStroke, 0.18, {
			Color = Color3.fromRGB(125, 128, 136),
			Transparency = 0.12
		}):Play()

		tween(label, 0.18, {
			TextColor3 = Color3.fromRGB(248, 249, 251)
		}):Play()

		tween(n, 0.18, {
			TextColor3 = Color3.fromRGB(190, 193, 200)
		}):Play()
	end)

	button.MouseLeave:Connect(function()
		tween(button, 0.18, {
			BackgroundTransparency = 0.12
		}):Play()

		tween(buttonStroke, 0.18, {
			Color = Color3.fromRGB(66, 69, 76),
			Transparency = 0.4
		}):Play()

		tween(label, 0.18, {
			TextColor3 = Color3.fromRGB(215, 217, 222)
		}):Play()

		tween(n, 0.18, {
			TextColor3 = Color3.fromRGB(105, 108, 115)
		}):Play()
	end)

	button.MouseButton1Click:Connect(function()
		if callback then
			callback()
		end
	end)

	return button
end

local function createPageLayout(page)
	local layout = Instance.new("UIListLayout")
	layout.Padding = UDim.new(0, 8)
	layout.SortOrder = Enum.SortOrder.LayoutOrder
	layout.Parent = page
	return layout
end

for _, page in pairs(pages) do
	createPageLayout(page)
end

local function createToggle(parent, text, callback)
	local enabled = false

	local button = Instance.new("TextButton")
	button.Size = UDim2.new(1, -10, 0, 45)
	button.BackgroundColor3 = CARD
	button.BorderSizePixel = 0
	button.Text = ""
	button.AutoButtonColor = false
	button.Parent = parent

	round(button, 11)

	local label = Instance.new("TextLabel")
	label.Position = UDim2.fromOffset(14, 0)
	label.Size = UDim2.new(1, -75, 1, 0)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = WHITE
	label.TextSize = 11
	label.Font = Enum.Font.GothamMedium
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = button

	local state = Instance.new("TextLabel")
	state.Name = "State"
	state.Position = UDim2.new(1, -58, 0, 0)
	state.Size = UDim2.fromOffset(45, 45)
	state.BackgroundTransparency = 1
	state.Text = "OFF"
	state.TextColor3 = MUTED
	state.TextSize = 10
	state.Font = Enum.Font.GothamBold
	state.Parent = button

	local function setEnabled(value)
		enabled = value
		state.Text = enabled and "ON" or "OFF"
		state.TextColor3 = enabled and GREEN or MUTED

		tween(button, .15, {
			BackgroundColor3 = enabled
				and Color3.fromRGB(25, 38, 29)
				or CARD
		}):Play()

		if callback then
			callback(enabled)
		end
	end

	button.MouseButton1Click:Connect(function()
		setEnabled(not enabled)
	end)

	return setEnabled
end

local function createSlider(parent, text, minValue, maxValue, defaultValue, callback)
	local value = defaultValue
	local draggingSlider = false

	local holder = Instance.new("Frame")
	holder.Size = UDim2.new(1, -10, 0, 62)
	holder.BackgroundColor3 = CARD
	holder.BorderSizePixel = 0
	holder.Parent = parent

	round(holder, 11)

	local label = Instance.new("TextLabel")
	label.Position = UDim2.fromOffset(14, 7)
	label.Size = UDim2.new(1, -70, 0, 20)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = WHITE
	label.TextSize = 11
	label.Font = Enum.Font.GothamMedium
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = holder

	local valueLabel = Instance.new("TextLabel")
	valueLabel.Position = UDim2.new(1, -55, 0, 7)
	valueLabel.Size = UDim2.fromOffset(40, 20)
	valueLabel.BackgroundTransparency = 1
	valueLabel.Text = tostring(value)
	valueLabel.TextColor3 = GREEN
	valueLabel.TextSize = 10
	valueLabel.Font = Enum.Font.GothamBold
	valueLabel.TextXAlignment = Enum.TextXAlignment.Right
	valueLabel.Parent = holder

	local bar = Instance.new("Frame")
	bar.Position = UDim2.fromOffset(14, 36)
	bar.Size = UDim2.new(1, -28, 0, 5)
	bar.BackgroundColor3 = Color3.fromRGB(45, 48, 55)
	bar.BorderSizePixel = 0
	bar.Parent = holder

	round(bar, 5)

	local fill = Instance.new("Frame")
	fill.Size = UDim2.fromScale(
		(value - minValue) / (maxValue - minValue),
		1
	)
	fill.BackgroundColor3 = GREEN
	fill.BorderSizePixel = 0
	fill.Parent = bar

	round(fill, 5)

	local function setValue(newValue)
		value = math.clamp(
			math.floor(newValue + .5),
			minValue,
			maxValue
		)

		local percent = (value - minValue) / (maxValue - minValue)

		fill.Size = UDim2.fromScale(percent, 1)
		valueLabel.Text = tostring(value)

		if callback then
			callback(value)
		end
	end

	local function update(x)
		local percent = math.clamp(
			(x - bar.AbsolutePosition.X) / bar.AbsoluteSize.X,
			0,
			1
		)

		setValue(minValue + ((maxValue - minValue) * percent))
	end

	bar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then

			draggingSlider = true
			update(input.Position.X)
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if not draggingSlider then
			return
		end

		if input.UserInputType == Enum.UserInputType.MouseMovement
			or input.UserInputType == Enum.UserInputType.Touch then

			update(input.Position.X)
		end
	end)

	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then

			draggingSlider = false
		end
	end)

	local function setInteractable(enabled)
		holder.BackgroundColor3 = enabled and CARD or Color3.fromRGB(30, 31, 35)
		label.TextColor3 = enabled and WHITE or DIM
		valueLabel.TextColor3 = enabled and GREEN or DIM
		fill.BackgroundColor3 = enabled and GREEN or DIM
		bar.BackgroundColor3 = enabled
			and Color3.fromRGB(45, 48, 55)
			or Color3.fromRGB(35, 36, 40)
	end

	return setInteractable, setValue
end

-- Popup

local popup = Instance.new("Frame")
popup.AnchorPoint = Vector2.new(1, 1)
popup.Position = UDim2.new(1, -20, 1, 65)
popup.Size = UDim2.fromOffset(245, 45)
popup.BackgroundColor3 = PANEL
popup.BackgroundTransparency = 1
popup.BorderSizePixel = 0
popup.Visible = false
popup.Parent = gui

round(popup, 11)

local popupStroke = stroke(
	popup,
	Color3.fromRGB(65, 68, 76),
	1
)

local popupText = Instance.new("TextLabel")
popupText.Size = UDim2.new(1, -20, 1, 0)
popupText.Position = UDim2.fromOffset(10, 0)
popupText.BackgroundTransparency = 1
popupText.TextColor3 = WHITE
popupText.TextSize = 11
popupText.Font = Enum.Font.GothamMedium
popupText.TextTransparency = 1
popupText.Parent = popup

local popupToken = 0

local function hidePopup()
	if not popup.Visible then
		return
	end

	popupToken += 1

	tween(
		popup,
		.22,
		{
			Position = UDim2.new(1, -20, 1, 65),
			BackgroundTransparency = 1
		},
		Enum.EasingStyle.Quad,
		Enum.EasingDirection.In
	):Play()

	tween(popupStroke, .18, {
		Transparency = 1
	}):Play()

	tween(popupText, .18, {
		TextTransparency = 1
	}):Play()

	task.delay(.25, function()
		if popup.Visible then
			popup.Visible = false
		end
	end)
end

function showPopup(text)
	popupToken += 1
	local token = popupToken

	popupText.Text = text
	popup.Visible = true
	popup.Position = UDim2.new(1, -20, 1, 65)
	popup.BackgroundTransparency = 1
	popupStroke.Transparency = 1
	popupText.TextTransparency = 1

	tween(
		popup,
		.3,
		{
			Position = UDim2.new(1, -20, 1, -20),
			BackgroundTransparency = 0
		},
		Enum.EasingStyle.Back
	):Play()

	tween(popupStroke, .25, {
		Transparency = .35
	}):Play()

	tween(popupText, .25, {
		TextTransparency = 0
	}):Play()

	task.delay(2, function()
		if token == popupToken and popup.Visible then
			hidePopup()
		end
	end)
end

-- Features

local character
local humanoid
local root

local function refreshCharacter()
	character = player.Character or player.CharacterAdded:Wait()
	humanoid = character:WaitForChild("Humanoid")
	root = character:WaitForChild("HumanoidRootPart")
end

refreshCharacter()

player.CharacterAdded:Connect(function()
	task.wait(.5)
	refreshCharacter()
end)

local cframeSpeed = false
local cframeAmount = 16

local flyEnabled = false
local flySpeed = 50

local noclipEnabled = false

local flyConnection
local noclipConnection
local speedConnection

local function stopSpeed()
	if speedConnection then
		speedConnection:Disconnect()
		speedConnection = nil
	end
end

local function startSpeed()
	stopSpeed()

	speedConnection = RunService.RenderStepped:Connect(function()
		if not cframeSpeed then
			return
		end

		if not character or not root or not humanoid then
			return
		end

		local direction = humanoid.MoveDirection

		if direction.Magnitude > 0 then
			root.CFrame = root.CFrame + direction * (cframeAmount / 60)
		end
	end)
end

local function stopFly()
	if flyConnection then
		flyConnection:Disconnect()
		flyConnection = nil
	end

	if humanoid then
		humanoid.PlatformStand = false
	end
end

local function startFly()
	stopFly()

	flyConnection = RunService.RenderStepped:Connect(function()
		if not flyEnabled then
			return
		end

		if not character or not root or not humanoid then
			return
		end

		humanoid.PlatformStand = true

		local camera = workspace.CurrentCamera
		local direction = Vector3.zero

		if UserInputService:IsKeyDown(Enum.KeyCode.W) then
			direction += camera.CFrame.LookVector
		end

		if UserInputService:IsKeyDown(Enum.KeyCode.S) then
			direction -= camera.CFrame.LookVector
		end

		if UserInputService:IsKeyDown(Enum.KeyCode.A) then
			direction -= camera.CFrame.RightVector
		end

		if UserInputService:IsKeyDown(Enum.KeyCode.D) then
			direction += camera.CFrame.RightVector
		end

		if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
			direction += Vector3.yAxis
		end

		if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
			direction -= Vector3.yAxis
		end

		if direction.Magnitude > 0 then
			root.AssemblyLinearVelocity =
				direction.Unit * flySpeed
		else
			root.AssemblyLinearVelocity = Vector3.zero
		end
	end)
end

local function stopNoclip()
	if noclipConnection then
		noclipConnection:Disconnect()
		noclipConnection = nil
	end

	if character then
		for _, obj in ipairs(character:GetDescendants()) do
			if obj:IsA("BasePart") then
				obj.CanCollide = true
			end
		end
	end
end

local function startNoclip()
	stopNoclip()

	noclipConnection = RunService.Stepped:Connect(function()
		if not noclipEnabled or not character then
			return
		end

		for _, obj in ipairs(character:GetDescendants()) do
			if obj:IsA("BasePart") then
				obj.CanCollide = false
			end
		end
	end)
end

-- TSB

createHubButton(tsbPage, "Supa V2", function()
	status.Text = "●  Loading Supa V2..."
	showPopup("Loading Supa V2")

	task.spawn(function()
		local success, err = pcall(function()
			loadstring(game:HttpGet(
				"https://api.getpolsec.com/scripts/hosted/2753546c83053761e44664d36ffe5035d6e20fc8aee1d19f0eb7b933974ae537.lua"
			))()
		end)

		if success then
			status.Text = "●  Supa V2 loaded"
			showPopup("Supa V2 loaded")
		else
			status.Text = "●  Supa V2 failed"
			showPopup("Supa V2 failed to load")
			warn("[Nocturnal] Supa V2:", err)
		end
	end)
end)

createHubButton(tsbPage, "Revenant", function()
	status.Text = "●  Loading Revenant..."
	showPopup("Loading Revenant")

	task.spawn(function()
		local success, err = pcall(function()
			loadstring(game:HttpGet(
				"https://gitlab.com/aristooey/Revenant/-/raw/main/Loader.lua"
			))()
		end)

		if success then
			status.Text = "●  Revenant loaded"
			showPopup("Revenant loaded")
		else
			status.Text = "●  Revenant failed"
			showPopup("Revenant failed to load")
			warn("[Nocturnal] Revenant:", err)
		end
	end)
end)

-- VBL

createHubButton(vblPage, "Zeck Hub", function()
	status.Text = "●  Loading Zeck Hub..."
	showPopup("Loading Zeck Hub")

	task.spawn(function()
		local success, err = pcall(function()
			loadstring(game:HttpGet(
				"https://raw.githubusercontent.com/Nocturnal-AM/My-Scripts/refs/heads/Loadstrings-for-hub/ZeckHub"
			))()
		end)

		if success then
			status.Text = "●  Zeck Hub loaded"
			showPopup("Zeck Hub loaded")
		else
			status.Text = "●  Zeck Hub failed"
			showPopup("Zeck Hub failed to load")
			warn("[Nocturnal] Zeck Hub:", err)
		end
	end)
end)


createHubButton(vblPage, "VBL General Data Reroll", function()
	status.Text = "●  Loading Data Reroller..."
	showPopup("Loading Data Reroller")

	task.spawn(function()
		local success, err = pcall(function()
			loadstring(game:HttpGet(
				"https://raw.githubusercontent.com/mrmangola/FurHub/main/FurHub"
			))()
		end)

		if success then
			status.Text = "●  Data reroller loaded"
			showPopup("Data reroller loaded")
		else
			status.Text = "●  Data reroller failed"
			showPopup("Data reroller failed to load")
			warn("[Nocturnal] Data Reroller:", err)
		end
	end)
end)

-- General

local speedToggle = createToggle(generalPage, "CFrame Speed", function(enabled)
	cframeSpeed = enabled

	if enabled then
		startSpeed()
	else
		stopSpeed()
	end
end)

local speedSliderEnabled, setSpeed = createSlider(
	generalPage,
	"CFrame Speed",
	1,
	100,
	16,
	function(value)
		cframeAmount = value
	end
)

speedSliderEnabled(false)

createHubButton(generalPage, "General Admin (VC bypass)", function()
	status.Text = "●  Loading General Admin..."
	showPopup("Loading General Admin")

		local success, err = pcall(function()
		    local source = game:HttpGet("https://mois7.xyz/loader")
		    local fn = loadstring(source)
		
		    if not fn then
		        error("loadstring returned nil")
		    end
		
		    fn()
		end)

		if success then
			status.Text = "●  General Admin loaded"
			showPopup("General Admin loaded")
		else
			status.Text = "●  General Admin failed"
			showPopup("General Admin failed to load")
			warn("[Nocturnal] General Admin:", err)
		end
	end)
end)


local flyToggle = createToggle(generalPage, "Fly", function(enabled)
	flyEnabled = enabled

	if enabled then
		startFly()
	else
		stopFly()
	end
end)

local flySliderEnabled, setFlySpeed = createSlider(
	generalPage,
	"Fly Speed",
	1,
	100,
	50,
	function(value)
		flySpeed = value
	end
)

flySliderEnabled(false)

local noclipToggle = createToggle(generalPage, "Noclip", function(enabled)
	noclipEnabled = enabled

	if enabled then
		startNoclip()
	else
		stopNoclip()
	end
end)

createHubButton(generalPage, "Infinite Yield", function()
	status.Text = "●  Loading Infinite Yield..."
	showPopup("Loading Infinite Yield")

	task.spawn(function()
		local success, err = pcall(function()
			loadstring(game:HttpGet(
				"https://pastefy.app/9bL14Crv/raw"
			))()
		end)

		if success then
			status.Text = "●  Infinite Yield loaded"
			showPopup("Infinite Yield loaded")
		else
			status.Text = "●  Infinite Yield failed"
			showPopup("Infinite Yield failed to load")
			warn("[Nocturnal] Infinite Yield:", err)
		end
	end)
end)

-- JJS

createHubButton(jjsPage, "JJS TBO", function()
	status.Text = "●  Loading JJS TBO..."
	showPopup("Loading JJS TBO")

	task.spawn(function()
		local success, err = pcall(function()
			loadstring(game:HttpGet(
				"https://raw.githubusercontent.com/cool5013/TBO/main/TBOscript"
			))()
		end)

		if success then
			status.Text = "●  JJS TBO loaded"
			showPopup("JJS TBO loaded")
		else
			status.Text = "●  JJS TBO failed"
			showPopup("JJS TBO failed to load")
			warn("[Nocturnal] JJS TBO:", err)
		end
	end)
end)


-- Tabs

local function selectTab(name)
	for tabName, data in pairs(tabs) do
		local selected = tabName == name

		pages[tabName].Visible = selected

		tween(data.Label, .2, {
			TextColor3 = selected and BG or MUTED
		}):Play()
	end

	local button = tabs[name].Button

	tween(
		activePill,
		.35,
		{
			Position = UDim2.fromOffset(
				button.Position.X.Offset,
				button.Position.Y.Offset
			)
		},
		Enum.EasingStyle.Quart
	):Play()
end

for name, data in pairs(tabs) do
	data.Button.MouseEnter:Connect(function()
		if not pages[name].Visible then
			tween(data.Label, .15, {
				TextColor3 = WHITE
			}):Play()
		end
	end)

	data.Button.MouseLeave:Connect(function()
		if not pages[name].Visible then
			tween(data.Label, .15, {
				TextColor3 = MUTED
			}):Play()
		end
	end)

	data.Button.MouseButton1Click:Connect(function()
		selectTab(name)
	end)
end

selectTab("Home")

-- Drag

local dragging = false
local dragStart
local startPosition

local function beginDrag(input)
	dragging = true
	dragStart = input.Position
	startPosition = main.Position

	input.Changed:Connect(function()
		if input.UserInputState == Enum.UserInputState.End then
			dragging = false
		end
	end)
end

title.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then
		beginDrag(input)
	end
end)

brand.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then
		beginDrag(input)
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if not dragging then
		return
	end

	if input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch then

		local delta = input.Position - dragStart

		main.Position = UDim2.new(
			startPosition.X.Scale,
			startPosition.X.Offset + delta.X,
			startPosition.Y.Scale,
			startPosition.Y.Offset + delta.Y
		)
	end
end)

-- glass colors

local GLASS_TOP = Color3.fromRGB(46, 48, 53)
local GLASS_MID = Color3.fromRGB(27, 29, 32)
local GLASS_BOTTOM = Color3.fromRGB(20, 22, 24)

local GLASS_BORDER = Color3.fromRGB(66, 69, 76)
local GLASS_HOVER = Color3.fromRGB(125, 128, 136)

-- glass gradient

local function applyGlassGradient(obj, rotation)
	local gradient = obj:FindFirstChild("NocturnalGradient")

	if not gradient then
		gradient = Instance.new("UIGradient")
		gradient.Name = "NocturnalGradient"
		gradient.Parent = obj
	end

	gradient.Rotation = rotation or 90

	gradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, GLASS_TOP),
		ColorSequenceKeypoint.new(.5, GLASS_MID),
		ColorSequenceKeypoint.new(1, GLASS_BOTTOM)
	})

	gradient.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, .05),
		NumberSequenceKeypoint.new(.5, .12),
		NumberSequenceKeypoint.new(1, .05)
	})

	return gradient
end

-- glass object
local function applyGlass(obj, radius)
	obj.BackgroundColor3 = GLASS_MID
	obj.BackgroundTransparency = 0.08

	round(obj, radius or 11)

	local s = obj:FindFirstChild("NocturnalStroke")

	if not s then
		s = Instance.new("UIStroke")
		s.Name = "NocturnalStroke"
		s.Parent = obj
	end

	s.Color = GLASS_BORDER
	s.Thickness = 1
	s.Transparency = .4

	applyGlassGradient(obj, 90)

	return s
end

-- main window
main.BackgroundColor3 = Color3.fromRGB(19, 21, 24)
main.BackgroundTransparency = 0.08

applyGlassGradient(main, 90)

mainStroke.Color = Color3.fromRGB(75, 78, 86)
mainStroke.Transparency = .3
mainStroke.Thickness = 1

-- sidebar

sidebar.BackgroundColor3 = GLASS_MID
sidebar.BackgroundTransparency = 0

applyGlassGradient(sidebar, 90)

local sidebarStroke = sidebar:FindFirstChildOfClass("UIStroke")

if sidebarStroke then
	sidebarStroke.Color = GLASS_BORDER
	sidebarStroke.Transparency = .4
	sidebarStroke.Thickness = 1
end

-- active tab

activePill.BackgroundColor3 = Color3.fromRGB(225, 227, 231)
activePill.BackgroundTransparency = 0

local activeGradient = activePill:FindFirstChild("NocturnalGradient")

if activeGradient then
	activeGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
		ColorSequenceKeypoint.new(.5, Color3.fromRGB(220, 222, 226)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(185, 188, 193))
	})

	activeGradient.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, .08),
		NumberSequenceKeypoint.new(.5, .18),
		NumberSequenceKeypoint.new(1, .3)
	})
end

local activeStroke = activePill:FindFirstChild("NocturnalStroke")

if activeStroke then
	activeStroke.Color = Color3.fromRGB(255, 255, 255)
	activeStroke.Transparency = .15
	activeStroke.Thickness = 1
end

-- profile card

applyGlass(profile, 14)

profile.BackgroundTransparency = 0

local profileStroke = profile:FindFirstChild("NocturnalStroke")

if profileStroke then
	profileStroke.Transparency = .32
end

-- avatar

avatar.BackgroundColor3 = Color3.fromRGB(27, 29, 32)
avatar.BackgroundTransparency = 0
avatar.ImageTransparency = 0

local avatarGradient = avatar:FindFirstChild("NocturnalGradient")

if avatarGradient then
	avatarGradient:Destroy()
end

local avatarStroke = avatar:FindFirstChildOfClass("UIStroke")

if not avatarStroke then
	avatarStroke = Instance.new("UIStroke")
	avatarStroke.Parent = avatar
end

avatarStroke.Color = GLASS_BORDER
avatarStroke.Transparency = .45
avatarStroke.Thickness = 1

-- status card

applyGlass(status, 11)

status.BackgroundTransparency = 0.08
status.Text = "●  Connected"
status.TextColor3 = Color3.fromRGB(75, 210, 115)
status.TextTransparency = 0
status.ZIndex = 10

local statusStroke = status:FindFirstChild("NocturnalStroke")

if statusStroke then
	statusStroke.Transparency = .4
	statusStroke.ZIndex = 9
end

for _, child in ipairs(status:GetChildren()) do
	if child:IsA("TextLabel") or child:IsA("TextButton") then
		child.ZIndex = 11
		child.TextTransparency = 0
	end
end

-- card style

local function styleCard(card)
	if not card or not card:IsA("Frame") then
		return
	end

	applyGlass(card, 11)
	card.BackgroundTransparency = 0

	local s = card:FindFirstChild("NocturnalStroke")

	if s then
		s.Transparency = .4
	end
end

-- toggle style

local function styleToggle(button, label)
	applyGlass(button, 11)
	button.BackgroundTransparency = 0

	local strokeObj = button:FindFirstChild("NocturnalStroke")

	button.MouseEnter:Connect(function()
		tween(button, .18, {
			BackgroundTransparency = 0
		}):Play()

		if strokeObj then
			tween(strokeObj, .18, {
				Color = GLASS_HOVER,
				Transparency = .12
			}):Play()
		end

		tween(label, .18, {
			TextColor3 = Color3.fromRGB(248, 249, 251)
		}):Play()
	end)

	button.MouseLeave:Connect(function()
		tween(button, .18, {
			BackgroundTransparency = 0
		}):Play()

		if strokeObj then
			tween(strokeObj, .18, {
				Color = GLASS_BORDER,
				Transparency = .4
			}):Play()
		end

		tween(label, .18, {
			TextColor3 = WHITE
		}):Play()
	end)
end

-- slider style

local function styleSliderHolder(holder)
	if not holder then
		return
	end

	applyGlass(holder, 11)
	holder.BackgroundTransparency = 0

	local holderStroke = holder:FindFirstChild("NocturnalStroke")

	if holderStroke then
		holderStroke.Transparency = .4
	end

	local bar

	for _, child in ipairs(holder:GetChildren()) do
		if child:IsA("Frame") and child.AbsoluteSize.Y <= 8 then
			bar = child
			break
		end
	end

	if not bar then
		return
	end

	bar.BackgroundColor3 = Color3.fromRGB(45, 48, 55)
	bar.BackgroundTransparency = 0

	local fill = bar:FindFirstChildOfClass("Frame")

	if fill then
		fill.BackgroundColor3 = Color3.fromRGB(235, 237, 240)
		fill.BackgroundTransparency = 0

		local gradient = Instance.new("UIGradient")

		gradient.Color = ColorSequence.new({
			ColorSequenceKeypoint.new(
				0,
				Color3.fromRGB(210, 212, 216)
			),
			ColorSequenceKeypoint.new(
				.5,
				Color3.fromRGB(245, 246, 248)
			),
			ColorSequenceKeypoint.new(
				1,
				Color3.fromRGB(190, 193, 198)
			)
		})

		gradient.Transparency = NumberSequence.new({
			NumberSequenceKeypoint.new(0, .05),
			NumberSequenceKeypoint.new(.5, .1),
			NumberSequenceKeypoint.new(1, .15)
		})

		gradient.Parent = fill
	end
end

-- style content

for _, page in pairs(pages) do
	for _, obj in ipairs(page:GetChildren()) do
		if obj:IsA("Frame")
			and obj ~= sidebar
			and obj ~= main
			and obj ~= content
			and obj ~= activePill then

			styleCard(obj)
		end
	end
end

-- style toggles

for _, page in pairs(pages) do
	for _, obj in ipairs(page:GetChildren()) do
		if obj:IsA("TextButton") then
			local label = obj:FindFirstChildOfClass("TextLabel")

			if label then
				styleToggle(obj, label)
			end
		end
	end
end

-- style sliders

task.defer(function()
	task.wait(.1)

	for _, page in pairs(pages) do
		for _, obj in ipairs(page:GetChildren()) do
			if obj:IsA("Frame") then
				local hasBar = false

				for _, child in ipairs(obj:GetChildren()) do
					if child:IsA("Frame")
						and child.AbsoluteSize.Y <= 8 then

						hasBar = true
						break
					end
				end

				if hasBar then
					styleSliderHolder(obj)
				end
			end
		end
	end
end)

-- text colors

welcome.TextColor3 = Color3.fromRGB(240, 241, 244)
welcomeSub.TextColor3 = Color3.fromRGB(115, 119, 128)

username.TextColor3 = Color3.fromRGB(240, 241, 244)
displayName.TextColor3 = Color3.fromRGB(125, 129, 138)

-- connected text

status.TextColor3 = GREEN

-- State

local minimized = false
local animating = false
local destroyed = false

-- State

local minimized = false
local animating = false
local destroyed = false

-- Minimize

local function minimizeHub()
	if minimized or animating or destroyed then
		return
	end

	animating = true
	minimized = true

	local target = UDim2.new(
		main.Position.X.Scale,
		main.Position.X.Offset,
		1,
		30
	)

	tween(
		main,
		.4,
		{
			Position = target,
			Size = UDim2.fromOffset(20, 20),
			BackgroundTransparency = 1
		},
		Enum.EasingStyle.Back,
		Enum.EasingDirection.In
	):Play()

	tween(mainStroke, .3, {
		Transparency = 1
	}):Play()

	task.wait(.42)

	if destroyed then
		return
	end

	main.Visible = false
	animating = false

	showPopup("Right Alt to open script again")
end

-- Close

local function closeHub()
	if animating or destroyed then
		return
	end

	animating = true
	destroyed = true

	showPopup("bye bye :D")

	local closeTween = tween(
		main,
		.45,
		{
			Size = UDim2.fromOffset(20, 20),
			BackgroundTransparency = 1
		},
		Enum.EasingStyle.Back,
		Enum.EasingDirection.In
	)

	closeTween:Play()

	tween(mainStroke, .35, {
		Transparency = 1
	}):Play()

	for _, descendant in ipairs(main:GetDescendants()) do
		if descendant:IsA("TextLabel")
			or descendant:IsA("TextButton") then

			tween(descendant, .25, {
				TextTransparency = 1
			}):Play()

		elseif descendant:IsA("ImageLabel") then

			tween(descendant, .25, {
				ImageTransparency = 1
			}):Play()

		elseif descendant:IsA("Frame")
			and descendant ~= main then

			tween(descendant, .25, {
				BackgroundTransparency = 1
			}):Play()
		end
	end

	closeTween.Completed:Wait()
	task.wait(1)

	if gui and gui.Parent then
		gui:Destroy()
	end
end

minimize.MouseButton1Click:Connect(minimizeHub)
close.MouseButton1Click:Connect(closeHub)

-- Restore

UserInputService.InputBegan:Connect(function(input, processed)
	if processed or destroyed then
		return
	end

	if input.KeyCode ~= Enum.KeyCode.RightAlt then
		return
	end

	if not minimized or animating then
		return
	end

	animating = true
	hidePopup()

	main.Visible = true

	main.Position = UDim2.new(
		main.Position.X.Scale,
		main.Position.X.Offset,
		1,
		30
	)

	main.Size = UDim2.fromOffset(20, 20)
	main.BackgroundTransparency = 0
	mainStroke.Transparency = 1

	for _, descendant in ipairs(main:GetDescendants()) do
		if descendant:IsA("TextLabel")
			or descendant:IsA("TextButton") then

			descendant.TextTransparency = 0

		elseif descendant:IsA("ImageLabel") then

			descendant.ImageTransparency = 0

		elseif descendant:IsA("Frame")
			and descendant ~= main then

			if descendant == activePill then
				descendant.BackgroundTransparency = 0
			elseif descendant == sidebar then
				descendant.BackgroundTransparency = .25
			elseif descendant == content then
				descendant.BackgroundTransparency = 1
			else
				descendant.BackgroundTransparency = 0
			end
		end
	end

	tween(
		main,
		.45,
		{
			Position = UDim2.fromScale(.5, .5),
			Size = MAIN_SIZE,
			BackgroundTransparency = .02
		},
		Enum.EasingStyle.Back
	):Play()

	tween(mainStroke, .35, {
		Transparency = .35
	}):Play()

	task.wait(.46)

	minimized = false
	animating = false
end)

-- Cleanup

gui.Destroying:Connect(function()
	stopSpeed()
	stopFly()
	stopNoclip()
end)
